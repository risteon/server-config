services:
  ddclient:
    image: ddclient
    build: ./image
    container_name: ddclient
    volumes:
      - config:/config
      - cmd:/cmd
    restart: unless-stopped

volumes:
  config:
    driver: anybox/buttervolume
    driver_opts:
      copyonwrite: "true"
  cmd:
    driver: anybox/buttervolume
    driver_opts:
      copyonwrite: "true"
